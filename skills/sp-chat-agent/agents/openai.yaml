interface:
  display_name: "SMART POLE Chat Enforcer"
  short_description: "Validate SMART POLE readiness and produce XML handoff"
  default_prompt: "Use $sp-chat-agent to score my prompt readiness, ask only blocking clarification questions, and output the final <master_prompt> XML block when ready."
